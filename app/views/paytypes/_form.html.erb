<%= form_for(@paytype) do |f| %>
  <% if @paytype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paytype.errors.count, "error") %> prohibited this paytype from being saved:</h2>

      <ul>
      <% @paytype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pay_type %><br />
    <%= f.text_field :pay_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
