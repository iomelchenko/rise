<%= form_for([@product, @product.subcode_prods.build]) do |f| %>
  <p>
    <%= f.label :"Введите код товара" %><br />
    <%= f.text_field :subcode %>
  </p>
  <p>
    <%= f.submit submit_text_button%>
  </p>
<% end %>