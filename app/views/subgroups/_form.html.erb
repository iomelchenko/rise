<%= form_for([@group, @group.subgroups.build]) do |f| %>
  <p>
    <%= f.label :"Наименование новой подгруппы" %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :"Описание" %><br />
    <%= f.text_area :describe %>
  </p>
  <p>
    <%= f.submit "Создать" %>
  </p>
<% end %>
